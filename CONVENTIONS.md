- Mensajes de commit: cuando un commit solo afecte a los subdirectorios `catalogo`, `frontend` o `prestamos`, su mensaje de commit empezará por el nombre del directorio. Ejemplo: `catalogo: update Libro model`.
- TypeScript:
  - No usar punto y coma al final de las lineas, salvo que sea imprescindible.
